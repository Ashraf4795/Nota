package com.nota.data.local.room

const val DATABASE_NAME = "nota_database"

// This constant is used to pass it to NoteDisplayFragment as a safe arg
// to indicate that the user want to add a new note, NOT editing an existing one.
const val ADD_NEW_NOTE = 0L